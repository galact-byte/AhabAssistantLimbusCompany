# 融合上游主分支更新

## 目标

把 `KIYI671/AhabAssistantLimbusCompany` 的 `main` 新增提交融合进 fork，并保持 v1.5.10 已有的自动化恢复、OCR 和彩色截图行为。

## 已确认范围

上游目标 `ddc22040d1baf3f86fcd94c5384eda038ab8d439`，共同基点 `41299bb`；新增 5 个提交，净变更为季节入口图像和 `module/automation/automation.py` 的缺模板、空截图、空裁剪保护。合并预检无文本冲突，但自动合并仍需审查。

## 要求与验收

- [x] 保留上游完整新增历史及季节入口资源；特征匹配接收空模板/截图/裁剪时安全返回。双父提交 `4d7718c` 的第二父提交为 `ddc2204`。
- [x] 保留本地彩色帧、OCR 缓存、业务截图单一恢复所有者、交互取消门；不引入上游旧版的强制杀进程/无限截图重试。
- [x] 新防护和本地路径有先红后绿或同等回归检查，整库测试及静态检查通过。
- [x] 不推送、不发布、不动 G 盘安装或配置；合并完成后由父任务做整体集成检查。
