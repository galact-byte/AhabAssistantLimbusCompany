# 修复选队归顶误判并融合上游更新

## 目标

让 v1.5.10 在编队列表已归顶时正确识别首项并继续经验本选队，同时融合 KIYI671/AhabAssistantLimbusCompany `main` 的新增更新，保留 fork 的无人值守恢复与停止逻辑。

## 已确认事实

- 2026-09-25 两次失败截图均在顶部显示“剧情关卡”，但列表解析将其排除，继而报“首行未完整归顶”；不能据此断言后台滚轮无效。
- fork `main` 为 `0745ff9`；上游 `main` 为 `ddc2204`，相对共同基点 `41299bb` 新增 5 个提交，净变更仅季节入口图片及特征匹配防护。合并预检无冲突。
- 当前工作区无已修改的代码文件；规划时 09-05、09-13 仍有未完成验收。2026-09-25 用户随后确认已实机测试且整体没有明显问题，明确要求归档；两项的未独立验证点和历史原生崩溃限制仍在各自任务记录中保留。

## 交付范围

- 子任务 `09-25-team-list-top-boundary`：修复首行解析与归顶误判，覆盖两张真实失败截图和正常行距边界。
- 子任务 `09-25-upstream-main-integration`：融合上游 `main` 的完整新增提交，核验新防护与本地截图、OCR、恢复逻辑共存。
- 父任务：审查合并后的完整差异，运行相关及全量回归，记录真实运行限制。

## 验收标准

- [x] 两张失败截图经相同 OCR/解析流程识别“剧情关卡”为首项，判定已归顶；被遮挡、缺行或标题错误的画面不能误判成功。
- [x] Git 历史保留双方祖先及上游目标提交 `ddc2204`，季节资源与特征匹配空输入防护均存在；本地恢复、彩色截图、OCR 缓存及选队逻辑不丢失。合并提交 `4d7718c` 双亲为 `0745ff9`、`ddc2204`。
- [x] 新旧相关测试与全量可运行测试通过，静态检查和变更审查无新增错误；不把离线回放冒充真实游戏滚轮验证。

## 不在本次范围

不改 G 盘发行目录或用户游戏配置，不发布、不推送，不扩大本次产品代码到 09-05 商店专项或 09-13 原生崩溃调查。依用户后续授权，仅更新两项旧任务的验收记录并归档；不把历史 ONNX 原生崩溃原因标为已确认。
