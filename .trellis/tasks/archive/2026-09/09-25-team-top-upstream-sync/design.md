# 技术设计

## 两条独立交付线

1. 选队：只在 `tasks/team_list.py` 调整标题与首项的几何边界，继续由 `TeamListPage.first_row_at_top` 和相邻行间距约束防止缺行误选；以真实截图 OCR 坐标建立回归用例，不更改 Windows 滚轮手势和编队配置。先修复再融合或先融合再修复均可，但同一最终树需全量验证。
2. 上游：以历史合并接入 `upstream/main@ddc2204`（而非复制零散提交），保留 fork 的恢复/取消/缓存代码，审查自动合并后新增的空模板缓存生存期及清理路径。季节图像采用上游版本；不迁移配置。

## 边界与回滚

不修改 G 盘运行副本、不触及 09-05/09-13 待验收任务；若自动合并产生新冲突，停下评估冲突语义，不用 `-X theirs` 覆盖本地改动。合并前保存 HEAD 标识；不使用强制重置，失败时仅撤销本次未提交合并并保留其他工作。未经同意不推送/发布。实机滚轮和季节奖励领取仍需真实环境验证。
