# v1.5.7 日志排查（只读研究）

## 范围与证据

- 活动任务已用 `python ./.trellis/scripts/task.py current --source` 核对，未启动任务、未改产品代码、未改运行目录。
- 原目录：`G:/BaiduNetdiskDownload/AALC_v1.5.7/AALC/logs`；完整扫描五份日志，共 109543 行，22332371 字节。原日志原样副本：`research/evidence/logs-20260912-133031/`；采集时间 2026-09-12 13:30:31（本机 UTC+8），各文件 SHA-256、大小、源 mtime 见同目录 `manifest.json`。五份文件复制期间大小和 mtime 均稳定。
- 本文日志引用行号对应原样快照，文件名不带目录时均指该快照目录。未取得截图，不将 OCR 记录当作截图或像素证据。
- 辅助分析（不是原始证据替代品）：`log-metrics.json`、`log-stage-spans.json`、`log-info-timeline.txt`、`log-alerts.txt`。

## 已确认的主要问题

### 1. 镜牢商店存在持续超过 107 分钟的离店无进展循环

- 商店开始：2026-09-12 10:48:42.976，`debugLog.log.1:11451`。
- 离店循环开始：10:50:00.737，`debugLog.log.1:11636`；确认模板相似度 0.31，离开按钮识别成功。
- 首次确认弹窗漏识别：10:50:05.024，`debugLog.log.1:11648`。后续持续交替识别商店主界面与包含“要离开商店吗？/取消/确认”的 OCR 文本，但离店确认模板持续只有 0.31，仍然点击右下角“离开”。
- 最后一次相同循环：12:37:47.451，`debugLog.log:3537`；12:37:51.827、12:37:54.438 出现 CONNECTING（3540、3542）；12:37:56.124 已识别第5层地图（3544），12:37:56.440 判定 `map_legend` 并结束商店（3553–3555）。商店总耗时 1:49:13.465。
- 12:40:46.336 领取 30 通行证经验（`debugLog.log:4057`）；12:41:12.810 完成第5次镜牢并开始下一次（4195–4196）。**流程最终恢复，但不能证实为自动恢复**：退出前没有 AALC 成功点击离店确认的日志，可能存在用户手动点击；日志不记录外部鼠标输入。

### 2. 常规寻路反复走失败兜底，产生固定数十秒额外开销

- 最新运行中 155 次“未定位到公交车”警告，155 次完整寻路阶段，累计寻路耗时 8965.040 秒，单次最长 66.610 秒。
- 典型例：12:38:04.115 开始寻路（`debugLog.log:3587`），12:38:05.951 未定位到公交车（3591），12:38:48.128 进入恢复分支（3727），12:38:51.041 寻路结束（3732），随后进入战斗。
- 这不是日志完全停止：2026-09-12 02:22:07 开始运行至结束记录，连续日志最大间隔仅 9.745 秒。应区分“持续空转/低效恢复”与线程冻结。

## 终止与非问题边界

- 全部五份日志无 ERROR/CRITICAL，共174条 WARNING，全部为公交车未定位；其中19条属于09-06，155条属于09-12。没有异常堆栈或卡死超时重启记录。
- 最新日志 12:52:11.125 进入下一商店（`debugLog.log:6536`），正常推进至购买；12:52:32.970 记录“正在终止脚本线程”（6595），随后清缓存、保存镜牢进度、保存窗口位置（6596–6599）。这证明走过停止路径，但仅凭日志不能断言由用户点击、热键还是程序调用触发，也不能断言 AALC 崩溃。
- 三处 >=15 秒日志空白均在旧运行结束/新运行前配置操作间：跨09-06至09-12，以及01:48→02:09、02:09→02:22。不能把它们当作本轮游戏卡死。

## 代码追踪与验证建议

原版/本地归属由主代理对真实上游目标提交对照后定论，本研究不擅自归咎上游。代码基线 `f221d4386826167adbcd668758a84e8284c1baee`，涉及文件哈希见 `code-reference-hashes.json`。

### 商店完整调用链与已复现的控制流根因

1. `tasks/mirror/mirror.py:407–412`：主循环识别商店，阻塞调用 `self.in_shop`；`1546–1547` 委托 `Shop.in_shop(self.floor)`。外层道中防卡死计数在 `460`，子调用不返回时无法执行。
2. `tasks/mirror/in_shop.py:1336–1413`：依次处理忽略商店、技能替换、治疗、售卖、购买、合成、强化，然后进入离店。异常本次不是这些步骤卡住：10:49:56.869 已执行离店前最后空白点击（`debugLog.log.1:11626`）。
3. `1415–1444`：离店设 `loop_count=30`、`auto.model="clam"`；每轮截图与网络重试后先检查地图，然后匹配确认、离开、治疗返回。**三个点击分支只要返回 True 就 continue，绕过 loop_count -= 1。** 本轮离开模板一直命中，预算永不消耗，扩大识别范围与回主界面保护均不可达。
4. `module/automation/automation.py:120–162,182–253`：`click_element` 成功只表示找到模板并发出鼠标动作，不表示游戏页面发生推进；默认阈值0.8。`find_image_element:710–788` 在 clam 区域匹配，成功/失败由模板分数决定，不验证状态变化。
5. `tasks/base/retry.py:227–286`：每次调用重新设置 start_time；无网络弹窗即迅速返回，不是跨离店循环的全局90秒看门狗。故它不能阻止本次长期空转。
6. `tasks/mirror/shop_presence.py:60–85`：只回答当前是地图/商店/未知，不识别离店弹窗，不消耗退出预算；它在12:37:56成功识别地图，未见商店/地图误判导致本次停顿的证据。

**只读可重复验证**：`reproduce-shop-leave-budget.py` 用 AST 提取当前真实 `in_shop` 方法，在假对象上模拟“确认模板失败、离开按钮总成功、地图尚未出现”。运行 `python .trellis/tasks/09-12-upstream-sync-stall-audit/research/reproduce-shop-leave-budget.py`，60次完成点击后仍然 clam、0次回主界面，第61次由探针终止。输出存 `shop-leave-budget-result.json`。这是纯控制流复现，不导入或操作真实游戏，不是实机修复验证。

**精确量化**：10:50:00至12:37:47窗口内，1435次确认模板检测、1435次离开模板检测、1435次AALC鼠标点击；717条OCR结果明确包含“要离开商店吗”。确认模板均为0.31。首末确认检测相差6466.714秒（1:47:46.714），至确认返回地图共6475.703秒（1:47:55.703）。前一商店10:32:33.518确认模板为0.819并正常通过（`debugLog.log.1:8921`）；不是0.79卡在0.8附近的问题，不能直接套用此前降阈修复。

**已证实与未证实分界**：
- 已证实：确认弹窗OCR可见但确认模板未命中；继续点底层离开；成功点击绕过有限预算，造成无界循环。
- 尚未证实：为何该次确认模板从历史0.819变0.31（UI位置、模板区域、图像变化或其他原因），是否由人工确认才恢复。没有截图不能认定具体布局变了，也不能把0.31位置当作真实按钮位置。

### 寻路完整分支与归因边界

- `mirror.py:1055–1133`：简单键盘模式未启用时，先尝试 `MirrorMap.get_next_step`，失败后3次默认距离、3次最远距离，再进入恢复分支；仅到恢复阶段才使用地图门闩+`keyboard_node_fallback`。
- `search_road.py:283–328`：公交车未定位即返回空列表，本轮全155次如此；尚未运行到ONNX节点检测，不能称为ONNX模型故障。
- 典型公交车分数0.53（`debugLog.log:3588–3591`）、最远距离0.65（3716），均远低0.8；模板失败的像素根因同样需真实截图，不能盲目降阈。
- 155次均出现“寻路出错, 尝试重进镜牢”，且均记录 `search_road.py:166` 的进入模板检测。典型12:38:50.967，该模板0.95，接着点击并结束（3730–3733）；说明**恢复文案不等于真的重进镜牢**，此例实际走键盘选择节点成功。累计8965.040秒是整个寻路阶段，不可全部称为可节省时间。
- 正常地图与暂停菜单OCR在寻路过程中交替（例如3595），日志不足以确定是谁打开菜单；不据此另立未经证实的输入缺陷。
- 现有 `test_mirror_search_road_recovery.py` 覆盖空公交车/空ONNX、键盘成功/失败、恢复顺序；未约束先花3+3轮鼠标失败的耗时。若考虑提前键盘兜底，需保留“可规划时尊重路线权重”的产品语义，交主代理/用户裁定兼容性，不能擅自将全部寻路改成无脑第一节点。

### 停止路径

`app/farming_interface.py:607–655,729–738`：按钮/停止快捷键可进入窗口恢复→stop_script→清缓存→记录进度。本次末尾符合这一链路；自然结束另走 `_on_script_finished`，不重置窗口。可合理排除“日志显示未捕获异常导致崩溃”的说法，但外部调用来源仍未记录，不把它说成确定的人工停止。

### 建议验证矩阵（研究建议，未实施）

1. 先为离店预算新增失败回归：确认模板缺失+底层离开始终命中，必须有限结束/自动恢复，不能永久卡住。
2. 在真截图上验证离店确认识别：正常0.819样本、此次失败样本；模板之外的文本后备必须带“要离开商店吗”语义与按钮几何约束，禁止全屏泛点“确认”。没有截图时只能先验证纯逻辑，不宣称识别兼容已证明。
3. 覆盖离店正常成功、弹窗存在、unknown、网络重试、截图失败、最终地图、恢复失败返回与外层失败传播；不能只增加次数后无条件返回成功。
4. 保留 `test_mirror_shop_presence.py` 的地图否决商店与legend=0.777规则；现有测试只检查辅助判定与源码调用，未执行完整离店无进展分支。
5. 寻路测试量化“公交车缺失→键盘进入”调用顺序，失败仍应恢复，不取消有路线缓存时的队伍权重规划。
6. 本研究没有修改实现、没有运行真实游戏、没有运行项目全量测试；唯一执行性验证为上述纯AST离店探针。主代理合并后需跑受影响及全量可用测试。

## 采集后复核

研究结束前重新计算五份源日志SHA-256，均与原样快照一致；截至复核仍止于2026-09-12 12:52:38.004。研究写入仅在当前任务 research/ 内。注意日志头包含本机路径、游戏标识及配置，证据仅本地保存；对外分享前需另行授权并检查隐私。
