# 集成审查与验证边界

## 融合结果
- 基线 f221d43，上游目标41299bb；正常未提交merge，5处冲突已解决并标记，无冲突标记残留。
- automation：保留RGB事件检测，叠加上游截图锁、监视器缓存与交互门。
- mirror/search_road：保留map legend 0.75、空ONNX/公交车保护、地图内键盘恢复及重进0.7门限；上游新连线匹配与Dijkstra采用原节点权重（事件1、战斗4）。权重及不可达图测试通过。
- theme_pack：融合困难层切换接口，保留地图门闩。负权重是相对优先级，并非禁选；未改用户权重策略。
- team_formation：40队定位与MuMu专用滑动，保留本地完整命名匹配；全部原测试仍通过。
- fork：origin仍为galact-byte；本次未改资源仓库归属或发布工作流。

## 已确认修复
1. 离店循环预算在所有continue之前；模板确认优先，中文OCR弹窗兜底需语义和几何证据。无确认坐标时不点底层离开。
2. 耗尽预算调用back_init_menu(allow_restart=False)，显式失败穿过Shop→Mirror异常→onetime_mir_process False。Mirror_task现有策略会保留队首重试、不增加完成计数；不是直接宣称本场成功或强制退出软件。
3. check_team覆盖总人数10–12。只修确认的候选遗漏，不声明#900/#904像素根因已经复现。

## 近期关闭报告核对
| Issue | 代码证据/结论 |
|---|---|
| #897 | f9d4d38楼层CLEAR计数与旧模板兜底；保留floor_3_exit判断，实机待验。 |
| #893 | 本地test_mirror_search_road_recovery仍通过；上游门限调整不是唯一修复。 |
| #889 | 与#900/#904同类现象，候选遗漏已补；不能由关闭状态认定实机根因。 |
| #851/#877 | 上游4669454与狂气换体循环改进已融入；本机消费体力路径未执行。 |
| #882 | 保存困牢奖励时执行一次为既有语义，上游补解释日志，并非取消次数配置。 |
| #864 | 上游工具路径初始化已融合。 |
| #858/#835/#845 | 自动战斗排除镜牢死亡判定、小指良单通排除剩余战力检查已融合。 |
| #863 | MuMu多版本DLL候选与ADB更新已融合，无对应模拟器实机验证。 |
| #801 | 构造镜牢前深拷贝队伍配置，与开放#800同类。 |
| #838 | 新模板连线算法已融合，未用无权重首节点替代。 |
| #802/#816 | 专用队伍滑动接口已融合，桌面和MuMu手势仍需实机验证。 |

## 补充检查
- #594/#635相关主题包恢复：截图失败与识别异常曾绕过计数，新增测试先失败后通过。预算前置且仅执行一次主页恢复；显式False在Mirror分支抛出任务错误，停止本轮楼层计时。
- 不调整用户路线权重、不跳过未确认页面、不对模拟器冻结做未经验证的强制修复。

## 实际检查
- pytest：208 passed（9.23秒），research/current-tests.txt。
- 聚焦Ruff通过；全受影响文件312条诊断，对照HEAD 304条、上游312条，无超出双方并集的新诊断，research/lint-baseline.json。
- compileall及git diff --check HEAD通过；无未解决冲突。

## 明确未验证
- 未打包、未启动GUI或执行真实游戏操作；G盘运行副本保持不变。
- 商店108分钟循环属实，但无对应连续截图，不能断言模板低分的视觉原因或最终是否人工离店。
- 209条开放报告已完成正文及模块聚类适用性分类，重点报告已核对代码。缺原始帧、配置或特定模拟器环境的以证据不足结案，不宣称已排除；范围不等同于全附件/评论审计或209项实机复现。
- 监视器测试覆盖暂停业务与两帧恢复、stop释放门；强制线程终止与真实截图阻塞的时序未端到端验证。
- merge尚未提交；原始日志在本地忽略目录，不应公开提交。
