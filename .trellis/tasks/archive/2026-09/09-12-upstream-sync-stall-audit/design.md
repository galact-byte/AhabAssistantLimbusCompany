# 技术设计

## 基线与融合
- 本地基线 f221d4386826167adbcd668758a84e8284c1baee，上游 KIYI671/AhabAssistantLimbusCompany main 固定 41299bb84dace71dbb57395f580ddb05988be7e2；origin 为 galact-byte，不改远端或资源所有者。
- merge-tree 只读预检有5个冲突文件，42个上游变动文件。本任务作为一次集成验收单元，不拆重复研究的子任务。
- 使用正常 merge 保留双方历史，不强制覆盖、不重写历史。实施阶段先 --no-commit，最终提交单独审批。
- automation：合并上游截图锁/缓存与本地彩色帧保留。
- mirror：保留地图门闩、键盘恢复及本地0.7重进门限，接入上游楼层/结算/困难层切换。
- search_road：采用上游新寻路数据流，保留空公交车、空ONNX和地图内键盘恢复保护。
- select_theme_pack：沿用上游难度切换接口，保留地图门闩；旧难度OCR逻辑按新接口兼容迁移而非整块覆盖。
- team_formation：保留本地命名队伍精确匹配，同时接入上游40队编队、模拟器滚动修正。

## 商店失进展
日志快照与代码探针已证实成功点击绕过退出预算；上游同样存在，关联 #711/#438。
- 每轮包括截图失败与点击成功都必须受有限尝试/时间预算约束。
- 优先处理离店弹窗；只有确认处于商店主界面才点离开。文本兜底需带离店语义和按钮位置约束，不全屏泛点确认、不盲降0.31门限。
- 超时走自动恢复，并核查恢复结果及外层传播，不伪报离店成功，不要求用户手动游戏操作。
- 截图不可用时仅声明逻辑验证，不宣称模板失配原因已确定。

## Issue 审核
已抓取209条开放Issue及最近100条更新记录（含PR），证据存 research。逐项筛出缺陷、聚类重复与过时版本，针对当前代码建立适用性矩阵；近期战败误判、结算、编队、MuMu连接、主题权重、寻路与商店优先深入。
分类：本地已修 / 上游修复待融合 / 本地证实待修 / 环境或证据不足 / 功能请求或过时。后两类不盲改。评论与附件按相关性读取，不能以标题或关闭状态作为修复证据。

## 风险与回滚
新上游增加全局重试监视器、模拟器与UI配置；重点检查输入互斥/退出生命周期和彩色帧。发现认证、安全或实质行为冲突暂停该项审批。
不改G盘运行目录、不部署、不发布、不推送；原始日志只本地保存，不纳入公开提交。合并期间失败可在确认工作区边界后撤销本次未提交merge，不清理其他任务或用户数据。
寻路已有155次低效恢复，但像素失配原因未知；先验证上游新算法，保留路线权重语义，不直接全局改成键盘首节点。
